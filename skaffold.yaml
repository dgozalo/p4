apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/dgozalo/p4:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/dgozalo/p4:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: p4
        chartPath: charts/p4
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/dgozalo/p4'
          image.tag: '{{.DIGEST_HEX}}'
